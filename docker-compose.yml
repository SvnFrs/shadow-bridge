services:
  shadow-bridge:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      LOG_LEVEL: "info"
      SEARCH_TIMEOUT_MS: "5000"
    volumes:
      - shadowbridge-data:/app/data

volumes:
  shadowbridge-data:
